# Uncomment this line to define a global platform for your project
platform :ios, '10.0'

source 'https://github.com/CocoaPods/Specs.git'
source 'git@github.com:aTreey/MyGitSpec.git'

use_frameworks!
use_modular_headers!

target 'A' do

  # local pods
  # 本地引用
#  pod "B_Category", :path => "../B_Category"
  
  
  # third part pods
  pod "CTMediator"

  # private pods
  pod "HandyFrame"
 
end
